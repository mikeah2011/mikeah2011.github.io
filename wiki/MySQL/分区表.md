# 数据库分区表

## 定义

MySQL 分区表（Partition Table）是将一张逻辑表的数据按规则分散存储到多个物理文件中的技术。对应用层透明，SQL 无需修改。分区表通过 **分区裁剪**（Partition Pruning）自动跳过无关分区，实现「分而治之」。

## 核心原理

### 四种分区类型

| 分区类型 | 适用场景 | 分区键要求 |
|---------|---------|-----------|
| **Range** | 按时间范围划分（月度、年度） | 整数或日期表达式 |
| **List** | 按枚举值划分（状态、地区） | 整数列或返回整数的表达式 |
| **Hash** | 均匀分布数据，无明显范围特征 | 整数列或表达式 |
| **Key** | 类似 Hash，MySQL 自动选择 Hash 函数 | 必须是整数列 |

### Range 分区：月度订单表

```sql
CREATE TABLE orders (
    id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    user_id BIGINT UNSIGNED NOT NULL,
    order_no VARCHAR(32) NOT NULL,
    total_amount DECIMAL(10,2) NOT NULL DEFAULT 0,
    status TINYINT NOT NULL DEFAULT 0,
    created_at DATETIME NOT NULL,
    PRIMARY KEY (id, created_at),  -- 分区键必须包含在主键中！
    INDEX idx_user_id (user_id)
) ENGINE=InnoDB
PARTITION BY RANGE (YEAR(created_at) * 100 + MONTH(created_at)) (
    PARTITION p202601 VALUES LESS THAN (202602),
    PARTITION p202602 VALUES LESS THAN (202603),
    PARTITION p202603 VALUES LESS THAN (202604),
    PARTITION pmax VALUES LESS THAN MAXVALUE
);
```

### 分区裁剪（Partition Pruning）

```sql
-- 只扫描 p202601 分区，跳过其他分区
SELECT * FROM orders 
WHERE created_at >= '2026-01-01' AND created_at < '2026-02-01';

-- EXPLAIN PARTITIONS 可验证裁剪效果
EXPLAIN PARTITIONS SELECT * FROM orders WHERE created_at = '2026-01-15';
-- partitions: p202601 ✓
```

### List 分区：按状态分区

```sql
PARTITION BY LIST (status) (
    PARTITION p_pending VALUES IN (0, 1),
    PARTITION p_active VALUES IN (2, 3),
    PARTITION p_completed VALUES IN (4, 5),
    PARTITION p_cancelled VALUES IN (6)
);
```

### Hash 分区：均匀分布

```sql
PARTITION BY HASH (user_id) PARTITIONS 8;
-- 将 user_id 均匀分布到 8 个分区
```

## 关键踩坑

### 1. 主键必须包含分区键

```sql
-- ❌ 错误：分区键 created_at 不在主键中
PRIMARY KEY (id)
PARTITION BY RANGE (YEAR(created_at) * 100 + MONTH(created_at)) ...

-- ✅ 正确：分区键必须是主键的一部分
PRIMARY KEY (id, created_at)
```

### 2. 唯一索引必须包含分区键

```sql
-- ❌ 错误：唯一索引不包含分区键
UNIQUE INDEX idx_order_no (order_no)

-- ✅ 正确：唯一索引包含分区键
UNIQUE INDEX idx_order_no (order_no, created_at)
```

### 3. 外键不支持分区表

分区表不能有外键约束，需要在应用层维护引用完整性。

### 4. 自动分区维护

```php
// Laravel Artisan 命令：自动创建未来分区
class CreatePartition extends Command
{
    public function handle()
    {
        $nextMonth = Carbon::now()->addMonth();
        $partitionName = 'p' . $nextMonth->format('Ym');
        $value = $nextMonth->addMonth()->format('Ym');
        
        DB::statement("
            ALTER TABLE orders REORGANIZE PARTITION pmax INTO (
                PARTITION {$partitionName} VALUES LESS THAN ({$value}),
                PARTITION pmax VALUES LESS THAN MAXVALUE
            )
        ");
    }
}
```

## 实战案例

来自博客文章：
- [MySQL 分区表实战：Range/List/Hash 分区——Laravel 月度订单分区策略与查询路由](/2026/06/05/2026-06-05-MySQL-分区表实战-Range-List-Hash-Laravel月度订单分区策略与查询路由/) - 完整实战指南
- [数据归档策略](数据归档策略.md) - 分区归档与冷热分离

## 相关概念

- [分库分表](分库分表.md) - 分区表 vs 分库分表的选型
- [数据归档策略](数据归档策略.md) - 分区与归档的结合
- [索引创建原则](索引创建原则.md) - 分区表的索引设计
- [主键设计](主键设计.md) - 分区键对主键设计的约束
- [数据库内定时任务](数据库内定时任务.md) - pg_cron + pg_partman 自动分区生命周期管理

## 常见问题

### Q: 分区表 vs 分库分表怎么选？
- 分区表：单机、MySQL 原生、运维简单、适合 1000 万以内
- 分库分表：分布式、跨节点、适合海量数据、复杂度高

### Q: 分区数有上限吗？
MySQL 限制最多 8192 个分区。实际建议不超过 100 个（过多分区影响 DDL 性能）。

### Q: 分区表能用普通索引吗？
可以。每个分区有独立的索引文件，效果等同于在小表上建索引。
