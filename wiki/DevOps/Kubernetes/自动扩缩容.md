# 自动扩缩容：HPA / VPA

## 定义

- **HPA（Horizontal Pod Autoscaler）**：根据指标自动调整 Pod 副本数量（横向扩容）
- **VPA（Vertical Pod Autoscaler）**：根据指标自动调整 Pod 的 CPU/Memory 资源请求（纵向扩容）

## 核心原理

### 为什么 CPU 指标在 Laravel 上经常误判？

PHP 类服务不一定是 CPU 型瓶颈。请求变慢时，CPU 可能还很低，但 **Pod 已经没有可接单能力**：

- PHP-FPM 子进程被慢 SQL 卡住
- 调第三方支付或库存接口超时
- Redis 热 key 抖动导致等待变长
- 单 Pod `pm.max_children` 已满，请求开始排队

### HPA 架构

```text
                      +-----------------------+
                      |  Ingress / Nginx LB   |
                      +-----------+-----------+
                                  |
                                  v
                    +-----------------------------+
                    |   Laravel API Deployment    |
                    |   php-fpm / nginx / app     |
                    +-------------+---------------+
                                  |
             +--------------------+--------------------+
             |                                         |
             v                                         v
   Metrics Server                              Prometheus + Adapter
   CPU / Memory                                自定义 Pod 指标
             |                                         |
             +--------------------+--------------------+
                                  v
                         HPA (autoscaling/v2)
                      CPU + php-fpm active process

                    VPA(recommendation only for API)
                        |
                        v
                 调整 requests/limits 基线

          Queue Worker Deployment --------------> 单独 HPA/VPA 策略
```

### HPA v2 配置（Laravel API）

```yaml
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: laravel-api-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: laravel-api
  minReplicas: 4
  maxReplicas: 20
  metrics:
  # CPU 指标（基础）
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  # 自定义指标：php-fpm active process（更贴近实际负载）
  - type: Pods
    pods:
      metric:
        name: php_fpm_active_processes
      target:
        type: AverageValue
        averageValue: "8"
  behavior:
    scaleUp:
      stabilizationWindowSeconds: 60
      policies:
      - type: Pods
        value: 2
        periodSeconds: 60
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
      - type: Percent
        value: 10
        periodSeconds: 60
```

### VPA 配置（推荐先用 recommendation 模式）

```yaml
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: laravel-api-vpa
spec:
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: laravel-api
  updatePolicy:
    updateMode: "Off"  # 先只看建议，不自动调整
  resourcePolicy:
    containerPolicies:
    - containerName: php-fpm
      minAllowed:
        cpu: 250m
        memory: 256Mi
      maxAllowed:
        cpu: 2000m
        memory: 2Gi
```

### 队列 Worker 独立策略

队列 Worker 的负载模式与 API 完全不同（CPU 可能不高，但内存占用大），需要独立的 HPA/VPA 策略：

```yaml
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: queue-worker-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: queue-worker
  minReplicas: 2
  maxReplicas: 10
  metrics:
  # 基于队列长度的自定义指标
  - type: External
    external:
      metric:
        name: redis_queue_length
        selector:
          matchLabels:
            queue: default
      target:
        type: AverageValue
        averageValue: "100"
```

## 实战案例

来自博客文章：
- [K8s HPA/VPA 自动扩缩容实战](/categories/DevOps/k8s-hpa-vpa-guide-laravel-api-cpu/) - CPU 误判到自定义指标扩容的踩坑记录
- [Kubernetes HPA 实战](/categories/DevOps/kubernetes-hpa-guide-laravel/) - 自动扩缩容策略

## 相关概念

- [K8s 基础](K8s基础.md) - Deployment 资源请求/限制配置
- [Ingress 与网络](Ingress与网络.md) - 扩缩容后的流量分配
- [Prometheus 监控告警](../Prometheus监控告警.md) - 自定义指标采集
- [渐进式发布](渐进式发布.md) - 扩缩容与发布策略的协同

## 常见问题

### HPA 不触发扩容
- 检查 Metrics Server 是否正常运行：`kubectl top pods`
- 检查 HPA 状态：`kubectl describe hpa`
- 确认 Deployment 的 resources.requests 已设置（HPA 基于 requests 计算百分比）

### VPA 自动调整导致 Pod 重启
- VPA 的 `Auto` 模式会重启 Pod 来应用新的资源请求
- 推荐先用 `Off` 模式观察建议值，再手动调整
- API Pod 建议用 VPA recommendation + 手动调整，不用 Auto 模式

### 扩缩容抖动（flapping）
- 增大 `stabilizationWindowSeconds`（稳定窗口）
- 调整 `scaleDown` 的策略，限制缩容速率
- 使用 `behavior` 字段精细控制扩缩容行为
