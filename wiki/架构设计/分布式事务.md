# 分布式事务（Distributed Transactions）

## 定义

分布式事务解决**多个独立服务之间如何保证数据一致性**的问题。在微服务架构下，每个服务有独立的数据库，本地事务无法跨越服务边界，需要专门的分布式事务方案。

## 核心原理

### 方案对比

| 方案 | 一致性 | 性能 | 复杂度 | 适用场景 |
|------|--------|------|--------|----------|
| **2PC（两阶段提交）** | 强一致 | 低 | 中 | 金融转账 |
| **TCC（Try-Confirm-Cancel）** | 强一致 | 中 | 高 | 资金类操作 |
| **Saga** | 最终一致 | 高 | 中 | 电商订单 |
| **本地消息表** | 最终一致 | 高 | 低 | 异步通知 |
| **事务消息（RocketMQ）** | 最终一致 | 高 | 中 | 消息驱动 |

### Saga 模式详解

Saga 将一个分布式事务拆分为**一系列本地事务**，每个本地事务有对应的**补偿操作**。如果某一步失败，按逆序执行已完成步骤的补偿操作。

#### 协同式（Choreography）：事件驱动

```
用户下单 → OrderService 发布 OrderCreated 事件
    ↓
InventoryService 监听到 → 扣减库存 → 发布 InventoryReserved 事件
    ↓
PaymentService 监听到 → 发起支付 → 发布 PaymentCompleted 事件
    ↓
NotificationService 监听到 → 发送通知
```

**优点**：服务间松耦合，无中心协调器
**缺点**：流程分散在各服务中，难以追踪和调试

#### 编排式（Orchestration）：中心协调

```
Orchestrator:
  Step 1 → 调用 InventoryService.reserve()
  Step 2 → 调用 CouponService.claim()
  Step 3 → 调用 OrderService.create()
  Step 4 → 调用 PaymentService.initiate()
  
  如果 Step 3 失败：
    Compensation → CouponService.revert()
    Compensation → InventoryService.release()
```

**优点**：流程清晰，易于监控和调试
**缺点**：Orchestrator 可能成为单点

### 补偿事务设计原则

| 原则 | 说明 |
|------|------|
| **幂等性** | 同一补偿操作执行多次结果相同 |
| **可交换性** | 补偿操作的执行顺序不影响最终结果 |
| **可重试** | 补偿失败后可安全重试 |
| **悬挂处理** | 补偿请求先于正向请求到达时的处理 |
| **空回滚** | 正向操作未执行时收到补偿请求 |

### 跨服务下单流程示例

```
订单创建涉及 5 个服务：
1. 库存服务 — 扣减商品库存
2. 优惠券服务 — 核销用户优惠券
3. 订单服务 — 创建订单记录
4. 支付服务 — 发起支付请求
5. 通知服务 — 发送下单成功通知

Saga 编排：
  Step 1: InventoryService.reserve()    ← 可补偿（release）
  Step 2: CouponService.claim()         ← 可补偿（revert）
  Step 3: OrderService.create()         ← 不可补偿（标记取消）
  Step 4: PaymentService.initiate()     ← 可补偿（refund）
  Step 5: NotificationService.send()    ← 无需补偿（最终一致性）
```

## 实战案例

来自博客文章：[分布式事务实战：Saga 模式在订单/库存/支付中的应用](/2026/05/16/distributedtransactionguide-saga/)

**踩坑经验**：
- B2C 电商允许「最终一致性」，Saga 性能远优于 2PC/TCC
- 每个步骤有明确的补偿操作，易于理解和维护
- 幂等性通过唯一业务 ID + 状态机实现

## 相关概念

- [微服务架构](微服务架构.md) - 分布式事务是微服务的核心挑战
- [事件驱动架构](事件驱动架构.md) - Saga 协同式基于事件驱动
- [CAP 定理](CAP定理.md) - 最终一致性是 CAP 的工程化选择
- [限流与高并发](限流与高并发.md) - 秒杀场景下的库存一致性

## 常见问题

**Q: 能不能避免分布式事务？**
A: 优先尝试。将需要一致性的操作放在同一个服务/聚合中。实在无法避免时再用 Saga。

**Q: Saga 和 TCC 怎么选？**
A: 电商/内容场景用 Saga（允许最终一致）；金融/资金场景用 TCC（需要强一致）。

**Q: 怎么保证补偿操作一定成功？**
A: 补偿操作设计为幂等 + 可重试，配合死信队列和人工介入兜底。
